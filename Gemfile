source "https://rubygems.org"

# Matches what GitHub Pages runs on its servers, so what you see
# locally is what you get when you push.
gem "github-pages", group: :jekyll_plugins

# Needed on newer Ruby versions.
gem "webrick"
